"""
Models for recommendations.
"""
from django.db import models

# Recommendations will be generated algorithmically, so minimal models for now
# We can add caching and recommendation history here later
